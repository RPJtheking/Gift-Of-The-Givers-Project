@model IEnumerable<GiftOfTheGiversProject.Models.Volunteer>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Username)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Availability)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ReliefProject)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Username)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Availability)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReliefProject.ReliefProject_ID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Volunteer_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Volunteer_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Volunteer_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
